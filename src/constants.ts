/**
 * Avoid using magic strings, especially ones that are used in multiple places.
 * Any change can be done centrally here instead of having to find all places
 * where a magic string is used to make the change.
 */

export const REPLACE_APPLICATION_NAME_AUTH0_AUDIENCE =
  'REPLACE_APPLICATION_NAME_AUTH0_AUDIENCE';
export const REPLACE_APPLICATION_NAME_AUTH0_ISSUER =
  'REPLACE_APPLICATION_NAME_AUTH0_ISSUER';

export const MAKE_EVENTS_ENABLED = 'MAKE_EVENTS_ENABLED';
